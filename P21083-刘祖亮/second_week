1、简要说明python垃圾回收机制
  python主要依靠GC模块引用计数器来进行垃圾回收。当内存某一块区域一定时间计数为0时，会触发垃圾回收。

2、什么是斐波那契数列、素数、质数和猴子吃桃问题？
斐波那契数列：是一个数列，从0(或1)开始第三个数等于前两个数之和，0 1 1 2 3 5 8 13
素数：又称质数，在大于1的自然数中，除了1和它本身不会被任何数整除。
猴子吃桃：猴子有若干个桃子，第一天吃了一半+1个，第二天又吃了剩下的一半+1个，以后明天吃前一天剩下的一半+1个，
到第十天想吃的时候，只剩下一个桃子了，问总共有多少个桃子？(陷阱：第九天吃完就发现只有一个桃子了)

3、请写出列表支持的所有方法和说明（例如：append向列表末尾添加元素）
append(object):向列表末尾添加元素，返回None(没有新列表产生，就地修改)
insert(index,object):根据index向列表中添加元素，index越靠前，越耗时,返回None,（索引超界：超越上界，尾部追加；超越下界，头部追加），尽量少用
index(value):索引，通过value来查找，从1开始，直到找到为止，如果列表中没有，会遍历列表，时间复杂度O(n),并抛出ValueError
conut(value):返回列表中匹配value的次数，会遍历列表，时间复杂度O(n)
len():返回列表元素个数，通过它本身记录，基本不耗费资源
extend(iteratable)->None:将可迭代对象追加进来
+ :将两个列表连接起来，产生新的列表，原列表不变
* ：重复操作，将本列表重复n次，返回新列表.(注意：list1[[0,1],[0,1],[0,1]],如果修改list1[0][1]=2,打印list1为[[2,1],[2,1],[2,1]])
remove(value):从左至右查找第一个匹配value的值，移除该元素，返回None，效率低
pop([index])：不指定index,移除尾部元素，指定index，从左至右移除第一个匹配value的值，索引超界抛出IndexError错误
clear():清除列表所有元素，剩下一个空列表
sort():对列表进行排序，默认升序,倒序sort(reverse=true)
