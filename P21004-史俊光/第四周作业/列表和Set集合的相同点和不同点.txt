2. 说明列表和Set集合的相同点和不同点
相同点：
都是可变的
都可以修改

不同点：
列表定义方法：list[]，set集合定义方法：set()
list[]是有序的，set集合是无序的
list[]可以去重，set集合不可以去重
list[]可以索引，set集合不可以索引
list[]元素可以是任意对象（数字，字符串，对象，列表等），set的元素要求必须可以hash（不可hash的类型有list,set)。

